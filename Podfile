# Uncomment the next line to define a global platform for your project
platform :ios, '12.0'

target 'SwiftGPRCTest' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!
  
  
  pod 'gRPC-Swift', '~> 1.8.0'
  pod 'SwiftProtobuf', '~> 1.0'
  pod 'SwiftNIO', '~> 2.40.0'
  # Pods for SwiftGPRCTest
  
  target 'SwiftGPRCTestTests' do
    # Pods for testing
  end
  
  target 'SwiftGPRCTestUITests' do
    # Pods for testing
  end
  
end
